# Specs

One Markdown file per feature, kebab-case: `{feature-name}.md`. Each file is
the authoritative description of what that feature does — read it before
changing the feature's behavior, update it in the same PR after.

See the `spec-driven-development` skill for the template, workflow, and
anti-patterns. Use `/spec` to create or update a spec.
