---
description: Retired — use the dmux-workflows / autonomous-agent-harness skills instead (not bundled in this install).
---

# /orchestrate (retired)

This command was retired from the default plugin command surface. Use the
`dmux-workflows and autonomous-agent-harness` skill instead — invoke it directly rather than typing
`/orchestrate`.

See `legacy-command-shims/README.md` for the full retired-command table.
