---
description: Retired — use the documentation-lookup skill instead.
---

# /docs (retired)

This command was retired from the default plugin command surface. Use the
`documentation-lookup` skill instead — invoke it directly rather than typing
`/docs`.

See `legacy-command-shims/README.md` for the full retired-command table.
