---
description: Retired — use the prompt-optimizer skill instead.
---

# /prompt-optimize (retired)

This command was retired from the default plugin command surface. Use the
`prompt-optimizer` skill instead — invoke it directly rather than typing
`/prompt-optimize`.

See `legacy-command-shims/README.md` for the full retired-command table.
