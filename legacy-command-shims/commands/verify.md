---
description: Retired — use the verification-loop skill instead.
---

# /verify (retired)

This command was retired from the default plugin command surface. Use the
`verification-loop` skill instead — invoke it directly rather than typing
`/verify`.

See `legacy-command-shims/README.md` for the full retired-command table.
