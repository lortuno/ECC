---
description: Retired — use the agent-sort skill instead.
---

# /agent-sort (retired)

This command was retired from the default plugin command surface. Use the
`agent-sort` skill instead — invoke it directly rather than typing
`/agent-sort`.

See `legacy-command-shims/README.md` for the full retired-command table.
