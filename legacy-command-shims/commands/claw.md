---
description: Retired — use the nanoclaw-repl skill instead (not bundled in this install).
---

# /claw (retired)

This command was retired from the default plugin command surface. Use the
`nanoclaw-repl` skill instead — invoke it directly rather than typing
`/claw`.

See `legacy-command-shims/README.md` for the full retired-command table.
