---
description: Retired — use the context-budget skill instead.
---

# /context-budget (retired)

This command was retired from the default plugin command surface. Use the
`context-budget` skill instead — invoke it directly rather than typing
`/context-budget`.

See `legacy-command-shims/README.md` for the full retired-command table.
