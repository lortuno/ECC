---
description: Retired — use the rules-distill skill instead.
---

# /rules-distill (retired)

This command was retired from the default plugin command surface. Use the
`rules-distill` skill instead — invoke it directly rather than typing
`/rules-distill`.

See `legacy-command-shims/README.md` for the full retired-command table.
