---
description: Retired — use the claude-devfleet skill instead (not bundled in this install).
---

# /devfleet (retired)

This command was retired from the default plugin command surface. Use the
`claude-devfleet` skill instead — invoke it directly rather than typing
`/devfleet`.

See `legacy-command-shims/README.md` for the full retired-command table.
